{
  "name": "dict",
  "bin": "./dist/app.js",
  "version": "1.0.0",
  "description": "A command line dictionary tool using words api.",
  "main": "app/app.js",
  "scripts": {
    "build": "babel ./app --out-dir dist/ --copy-files",
    "start": "npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gagan Lohia",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "babel-polyfill": "^6.26.0",
    "cli-spinner": "^0.2.10",
    "colors": "^1.4.0",
    "commander": "^4.1.1",
    "inquirer": "^7.0.6",
    "install": "^0.13.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GaganLohia/dictionary-cmd.git"
  }
}
